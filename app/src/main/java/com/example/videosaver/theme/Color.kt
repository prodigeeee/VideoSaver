package com.example.videosaver.theme

import androidx.compose.ui.graphics.Color

// ─── Deep dark backgrounds ────────────────────────────────────────────────────
val Background        = Color(0xFF0C0C0E)   // Near-black, slightly warm
val SurfaceDark       = Color(0xFF141416)   // Cards background
val SurfaceMid        = Color(0xFF1C1C1F)   // Elevated surfaces
val SurfaceHighlight  = Color(0xFF242428)   // Hover/pressed state

// ─── Amber/Gold accent (inspired by Aston Martin UI) ─────────────────────────
val Amber             = Color(0xFFE8A84A)   // Main accent
val AmberLight        = Color(0xFFF5C97C)   // Lighter accent (text on dark)
val AmberDim          = Color(0xFF8A5E1A)   // Dim accent (borders, inactive)
val AmberGlow         = Color(0x30E8A84A)   // Glow overlay (subtle)

// ─── Complementary accents ───────────────────────────────────────────────────
val TealAccent        = Color(0xFF4ACCE8)   // Used for audio-only indicator
val PurpleAccent      = Color(0xFF9B7BE8)   // Used for completed state
val SuccessGreen      = Color(0xFF4AE889)   // Download complete
val ErrorRed          = Color(0xFFE84A4A)   // Error state

// ─── Text ─────────────────────────────────────────────────────────────────────
val TextPrimary       = Color(0xFFF0EEDE)   // Warm white
val TextSecondary     = Color(0xFF8A8478)   // Muted warm gray
val TextDisabled      = Color(0xFF4A4540)   // Disabled / placeholder

// ─── Glass overlay ────────────────────────────────────────────────────────────
val GlassWhite        = Color(0x12FFFFFF)   // Glassmorphism card fill
val GlassBorder       = Color(0x1AFFFFFF)  // Glassmorphism card border
val GlassAmber        = Color(0x18E8A84A)   // Amber-tinted glass
